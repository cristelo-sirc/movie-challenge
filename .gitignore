# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Local scripts (not needed for deployment)
scripts/

# One-off analysis scripts, the data builder, and the obsolete UI preview were
# moved to archive/ (June 2026) and kept there as history rather than ignored.

# Local secrets (never commit)
.tmdb_key

# GitHub push token (never commit)
.github_token
package-lock.json
