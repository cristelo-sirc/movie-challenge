# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Local scripts (not needed for deployment)
scripts/

# Data builder (not needed for end users)
data-builder.html

# Local secrets (never commit)
.tmdb_key

# GitHub push token (never commit)
.github_token
